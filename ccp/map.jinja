
{%- load_yaml as base_defaults %}
Debian:
  pkgs:
  - python3-dev
  - python3-pip
  - python-virtualenv
  - python-netaddr
  - python-setuptools
  - gcc
  source:
    engine: git
    address: https://git.openstack.org/openstack/fuel-ccp
    port: 8123
  dir:
    base: /srv/ccp
  config:
    engine: local
  user: ccp
  images: {}
  versions: {}
  replicas: {}
  files: {}
{%- endload %}

{%- set control = salt['grains.filter_by'](base_defaults, merge=salt['pillar.get']('ccp:control')) %}
